# -----------------------------------------------------------------------------
# Secure Math Platform - Production Environment Variables Template
# -----------------------------------------------------------------------------
# Instructions:
# 1. Copy this file to `.env` in the same directory (`cp .env.prod.template .env`).
# 2. Fill in all the variables required, specifically the secure passwords and AWS keys.
# 3. DO NOT commit the populated `.env` file to version control.
# -----------------------------------------------------------------------------

# --- Database Credentials ---
# Change these values to secure passwords for the production database.
DB_USERNAME=securemath_admin
DB_PASSWORD=YOUR_SECURE_DATABASE_PASSWORD_HERE

# --- JWT Configuration ---
# Generate a strong, random 256-bit+ base64 encoded string for the secret.
# Example generation command: `openssl rand -base64 64`
JWT_SECRET=YOUR_SECURE_JWT_SECRET_HERE
JWT_EXPIRY_HOURS=24

# --- Application Security ---
# This is the password for the very first admin account created during the baseline setup.
# The system forces the admin to change it on the first login.
APP_INITIAL_ADMIN_PASSWORD=YOUR_INITIAL_ADMIN_PASSWORD_HERE

# --- AWS & Storage Configuration ---
# Create an IAM User in AWS with programmatic access and the S3 policy generated
# by the Terraform scripts, or leave blank if relying entirely on the EC2 IAM Instance Profile.
# (If using the IAM Profile from Terraform, these keys might not be strictly necessary 
# if the AWS SDK uses the default provider chain, but can acts as a fallback or for local dev).
AWS_ACCESS_KEY_ID=YOUR_AWS_ACCESS_KEY_ID_HERE
AWS_SECRET_ACCESS_KEY=YOUR_AWS_SECRET_ACCESS_KEY_HERE
AWS_REGION=us-east-1

# Set this to the exact name of the S3 bucket created by the Terraform script.
# Example formatting: secure-math-media-prod-xxxxxx
S3_BUCKET_NAME=YOUR_S3_MEDIA_BUCKET_NAME_HERE

# --- Video Security Limits ---
# Duration a signed HLS URL is valid for playback.
SIGNED_URL_EXPIRY_HOURS=2
# Maximum number of authorized devices allowed per student.
DEVICE_LIMIT=2

# --- Frontend Configuration ---
# The base URL for the API. In the default Docker Compose + Nginx setup, 
# requests to /api are proxied to the backend automatically.
VITE_API_BASE_URL=/api
